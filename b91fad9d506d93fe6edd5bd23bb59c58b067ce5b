lisa-addressed:
---
Comment (.github/workflows/test.yml:20):
This PR removes the `Run OPA Check` step, so CI now only runs `opa test policies`. Unit tests don't catch every parse/type/unsafe-var error that `opa check` does — a policy that compiles-but-is-wrong, or a future edit with no test, can now merge green. The README and `make validate` still reference `opa check`, so CI is now *less* strict than the documented local flow. Please restore the check step, ideally as `opa check --strict policies` (it passes today).
---
Comment (policies/elbv2_information_movement.rego:43):
Defaults should live in **one** place: `data.json`. This is the only policy that also declares inline `default … := []` fallbacks (lines 43-44), while `elbv2_tls_policy_approved.rego:43` and `elbv2_listener_https_enforcement.rego:46` read `data.*` directly with no fallback. Two consequences: (a) the source of defaults is inconsistent across the bundle, and (b) the inline defaults here are dead anyway since `data.json` always defines these keys. Please remove the `default approved_listener_protocol_values := []` / `default approved_listener_port_values := []` declarations and rely solely on `data.json` so there is a single declarative source of defaults that `policy_data` overrides.
---
Comment (.github/workflows/test.yml:19):
`opa fmt -l policies` currently flags `elbv2_information_movement.rego` (blank-line spacing around the `default` declarations), so the tree is already unformatted. Please run `opa fmt -w policies` to fix it, and add a **format check** stage to this workflow that runs on every PR — e.g. `opa fmt --diff policies` (or `opa fmt --fail policies`) which exits non-zero on any diff — so formatting drift is caught in CI rather than slipping in unnoticed.
Implementation guidance: Restore the OPA static check in .github/workflows/test.yml. After the Setup OPA step and before Run OPA Tests, add a separate step named Run OPA Check with `run: opa check --strict policies`. Keep the existing `opa test policies` step. Do not weaken the local/documented validation flow or replace tests with check; CI should run both strict check and tests.
Implementation guidance: In policies/elbv2_information_movement.rego, remove only the two inline fallback declarations `default approved_listener_protocol_values := []` and `default approved_listener_port_values := []`. Leave `approved_listener_protocol_values := data.compliance_framework.elbv2_information_movement.approved_listener_protocols` and `approved_listener_port_values := data.compliance_framework.elbv2_information_movement.approved_listener_ports` as the sole source for those defaults, because policies/data.json already defines both keys as empty arrays. Preserve the existing opt-in behavior: empty arrays mean unrestricted for that dimension, and only non-empty lists enable filtering.
Implementation guidance: Run `opa fmt -w policies` so policies/elbv2_information_movement.rego and any other policy files are formatted. Then update .github/workflows/test.yml to add a formatting check that runs on every workflow invocation, after Setup OPA and before OPA Check/OPA Tests. Prefer a step named Run OPA Format Check with a command that fails on formatting drift, such as `opa fmt --fail policies` if supported by the pinned OPA 1.6.0 CLI; otherwise use `opa fmt --diff policies` followed by a git diff exit check so CI fails when formatting would change files. Keep the OPA check and test steps separate.
